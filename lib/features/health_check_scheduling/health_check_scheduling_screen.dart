import 'package:flutter/material.dart';

class HealthCheckSchedulingScreen extends StatelessWidget {
  const HealthCheckSchedulingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: const Text('Health Check Scheduling'),
      // ),
      body: const Center(
        child: Text('Health Check Scheduling Screen'),
      ),
    );
  }
}
