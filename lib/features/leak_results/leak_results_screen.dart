import 'package:flutter/material.dart';

class LeakResultsScreen extends StatelessWidget {
  const LeakResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LeakResultsScreen'),
      ),
    );
  }
}
